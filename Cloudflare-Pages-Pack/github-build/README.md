# Git Build Path
1) Put YOUR SOURCE here (if you want).
2) Ensure package.json has a build script (Vite: `vite build`, CRA: `react-scripts build`).
3) Push to GitHub; connect repo in Cloudflare Pages.
